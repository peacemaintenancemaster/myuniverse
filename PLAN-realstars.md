# Stellia — 실제 별 데이터 기반 구현 기획 (PLAN.md 부록 A)

> 질문: "웹 기반 앱으로 실제 우주 지도를 가져와서(2D도 좋음) 진짜 있는 별이 드러나고
> 거기에 이름 붙이는 식으로 구현 가능한가? 현실성 검토 + 본질 놓치지 않는 종합 기획."

---

## 0. 한 줄 결론

**가능하다. 그리고 본질을 해치는 게 아니라 강화한다.**

PLAN.md의 핵심 선언은 이것이다 — *"우리는 별을 만들지 않아요. 성운에 가려진 걸 드러낼 뿐이에요."*
지금 프로토타입은 답할 때마다 **랜덤 좌표에 가짜 별을 생성**한다(`store/universe.ts`의 `randomStarPosition`).
이건 사실 선언과 모순된다 — 실제로는 "만들고" 있으니까.

실제 별 카탈로그를 쓰면 별은 **이미 하늘에 존재하던 진짜 별**이 되고, 답하기는 그걸 *생성*이 아니라
*드러내는* 행위가 된다. 카피("성운 속에 있던 별이에요")가 비유가 아니라 **사실**이 된다.
HR 도표 색온도 이스터에그도 가짜 근사가 아니라 실제 B-V 색지수가 된다.

난이도는 낮다. 무료·개방 카탈로그가 작고(수 MB 이하), 렌더링은 지금 엔진(R3F)이 이미 한다.

---

## 1. 현실성 검토

### 1-1. 데이터 — 어디서 진짜 별을 가져오나

| 카탈로그 | 별 수 | 내용 | 라이선스 | 용도 |
|---|---|---|---|---|
| **Yale Bright Star Catalog (BSC5)** | ~9,110 (육안 가시, mag<6.5) | RA/Dec, 등급, B-V 색지수, 분광형, **고유명**(Sirius·Vega 등 ~300개) + Bayer/Flamsteed 명 | 퍼블릭 도메인 | **드러낼 "이름 있는" 별** (메인) |
| **HYG Database v3** (astronexus) | ~119,000 | 위 + 거리(parallax)·절대등급·xyz 좌표 | CC BY-SA | 배경 "숨은 별 밭" (옵션) |
| Gaia DR3 | 18억 | 과함. 불필요 | — | ✗ |

- **권장**: 드러나는 별은 **BSC5**(퍼블릭 도메인, 고유명 보유, 9천 개면 평생 못 다 씀 — 주 1개면 174년치).
  배경 밭은 BSC5 그대로 쓰거나, 더 촘촘히 원하면 HYG에서 mag<7 필터(~16,000개).
- BSC5는 JSON 변환본이 이미 공개돼 있음(예: `brettonw/YaleBrightStarCatalog`, MIT). 한 번 받아 `public/data/stars.json`으로 가공.
- 가공 후 크기: 9,110개 × {ra, dec, mag, ci, name?} ≈ **200~400KB(gzip)**. 모바일에서도 즉시 로드.

### 1-2. 좌표 → 화면

- 카탈로그는 **적경(RA, 0~24h)·적위(Dec, -90~+90°)**를 준다 = 천구상의 진짜 위치.
- **3D안(권장, 변경 최소)**: RA/Dec를 구면 좌표로 변환해 큰 구의 *안쪽 면*에 별을 박는다.
  카메라가 구 안에서 회전 = 실제 밤하늘을 올려다보는 것. 지금 `BackgroundStars`의 구면 분포 로직을
  실제 좌표로 바꾸기만 하면 됨. "2D처럼 보이는 진짜 하늘"이 공짜로 나온다.
- **순수 2D안**: 스테레오그래픽/방위각 투영으로 (RA,Dec)→(x,y) 평면 천구도(planisphere).
  사용자가 "2D도 좋다"고 한 그 그림. 더 가볍고 모바일 친화적이지만 지금의 몰입형 3D 비행감은 잃음.
- **결론**: 3D 구면안으로 가되, 카메라를 좁은 FOV·낮은 회전속도로 두면 체감은 2D 별지도다.
  나중에 "내 우주 지도(2D 평면)" 보조 뷰를 프리미엄 내보내기용으로 추가.

### 1-3. 색 — 가짜 근사 → 진짜 항성물리

지금: 답변 *글자 수*로 색 결정(`starColorFromAnswer`). 이스터에그 "별 색=표면온도"가 사실은 거짓.
바꿈: 각 별의 **B-V 색지수 → 표면온도(K) → RGB**. 표준 변환식 공개돼 있음(Ballesteros 공식 등).
이러면 하늘의 모든 별이 *실제 색*으로 빛난다. 이스터에그가 진짜가 된다.

### 1-4. 성능

- 9천~1.6만 개 별은 `THREE.Points` 한 번에 그리면 모바일 GPU도 여유. (지금 이미 4,000개 셰이더로 돌림.)
- "드러난(이름 붙은) 별"만 개별 `StarMesh`(글로우·히트영역)로, 나머지는 Points 밭으로 — 2-레이어.

### 1-5. 본질 정합성 — 오히려 강해지는 지점들

| PLAN.md 선언 | 랜덤 별 | **실제 별** |
|---|---|---|
| "별을 만들지 않고 드러낼 뿐" | 모순(생성함) | **사실이 됨** |
| 별 색 = 표면온도(이스터에그) | 가짜 근사 | **진짜 B-V** |
| 빛의 속도 = 과거를 본다 | 임의 | **진짜 거리(광년) 표시 가능** |
| 별자리는 인간이 만든 것 | — | 유저 선이 **진짜 IAU 별자리 경계를 가로지름** = 이스터에그 |
| 암흑물질 = 안 드러난 마음 | 빈 공간 | **흐릿한 진짜 별 밭**(아직 안 드러난 카탈로그 별) |

### 1-6. 리스크 — 본질을 잃는 단 하나의 길

> **실제 별을 쓰는 순간, 천문학 교육앱으로 변질될 유혹이 생긴다.** 그게 유일한 진짜 위험.

Stellia는 *밤하늘*에 대한 앱이 아니라 *나 자신*에 대한 앱이다. 그래서:
- 화면에 절대 안 보이는 것: "베가 / 거문고자리 / 25광년" 같은 **천문 라벨, 별자리 이름, 좌표, 등급 숫자**.
- 진짜 별 데이터는 **하부 기질(substrate)·이스터에그**일 뿐, 절대 전면 주제가 아니다.
- 별의 *진짜 이름*은 유저가 자기 이름을 붙인 **한참 뒤** "이 별은 옛날부터 베가라고 불렸대요"로만,
  조용히, 옵션으로 등장(천문 이스터에그 #2의 변주). 먼저 들이밀면 안 됨.

---

## 2. 핵심 설계 결정 (실제 별 × 본질)

### 2-1. 두 레이어 — 절대 분리 (PLAN 4-1과 동형)

```
숨은 별 밭 (전체 카탈로그, 진짜 위치·진짜 색, 아주 흐릿)   ← "암흑물질 / 아직 성운 속 별"
        │  주간 질문에 답함 = 성운 한 구역 흩어짐
        ▼
드러난 별 (그 구역의 실제 별 하나가 밝아지고 글로우·이름 획득)  ← 유저의 답 = 별 1개
```

핵심: **답하기 전에도 진짜 별은 거기 있다(흐리게).** 답하기는 *생성*이 아니라 *점등*이다.
이게 "성운에 가려진 걸 드러낼 뿐"의 문자 그대로의 구현.

### 2-2. 어떤 별이 드러나나 — 답의 결이 별을 *고른다*

랜덤이 아니라 **답의 감정 톤 → 그에 맞는 실제 별을 선택**:
- Claude API로 답에서 감정 강도/톤 추출(PLAN 4-3 임베딩 파이프 재사용).
- 격렬한 답 → 성운 구역 내 **뜨거운 파란 별(O/B형, B-V 낮음)** 점등.
- 차분한 답 → **따뜻한 금빛 별(K/M형, B-V 높음)** 점등.
- 이러면 HR 색온도 이스터에그가 *진짜 별 + 진짜 감정 매칭*으로 작동. 가짜 색칠이 사라진다.
- 같은 성운 구역의 미점등 별 중에서 고르므로 별자리(인접 별 잇기)도 자연히 성립.

### 2-3. 이름 — 유저가 짓고, 진짜 이름은 이스터에그

- 점등 즉시 별은 **이름 없음**. 탭하면 기억 카드(PLAN 7-2). 유저가 원할 때 **자기 이름**을 붙임.
- 본질: *내가 의미를 부여한 별.* 천문 고유명은 처음엔 절대 안 보임.
- 한참 뒤(재방문·시즌 편지 시점), 그 별이 마침 고유명 보유 별이면 조용히:
  *"이 별은 오래전부터 베가라 불렸대요. 당신은 이걸 '○○'이라 불렀고요."* — 두 이름의 공존이 울림.

### 2-4. 별자리 — 유저가 긋고, 진짜 경계를 가로지른다

- 앱은 IAU 별자리 선을 절대 미리 긋지 않음(PLAN 이스터에그 #5 그대로).
- 유저가 자기 별들을 이으면 그 선이 **진짜 거문고자리·백조자리 경계를 무심히 가로지름.**
  → 천문 이스터에그: "별자리는 인간이 만든 것"이 *체험으로* 증명됨. 인지 재구성 = 별자리 긋기.

### 2-5. 개인화 모델 — 모두 같은 하늘인데 어떻게 "나만의 우주"인가? (열린 결정)

실제 하늘은 하나뿐. PLAN의 셀링포인트는 "나만의 우주". 세 가지 해법:

| 안 | 방식 | 장점 | 단점 |
|---|---|---|---|
| **A. 공유 하늘·고유 점등(권장)** | 모두 같은 진짜 하늘, 단 **각자 점등한 별·이은 별자리·붙인 이름이 다름** | 데이터 단순, "같은 하늘 아래 다른 우주"라는 시적 진실 | 빈 하늘은 남과 같음 |
| B. 고유 시드 회전/구역 | 가입 시 천구를 임의 회전 → 각자 다른 패치를 "내 하늘"로 | 빈 하늘도 고유 | 진짜 좌표의 의미 약화 |
| C. 임베딩 결정 좌표 | 답 임베딩 → 천구 위 결정적 위치의 별 점등 | 답과 별의 의미적 연결 | 구현 복잡, 군집 편향 |

**권장 A**: "우리는 같은 하늘을 보지만, 당신이 드러낸 별·이은 선·붙인 이름은 당신만의 것."
이게 본질("발견·의미부여")과 가장 정합. (B는 프리미엄 변주로 후순위 가능.)
→ *이 선택은 사용자 확인 필요. 8장 참조.*

---

## 3. 데이터 파이프라인

1. BSC5 JSON 원본 확보(공개 변환본 또는 원자료 파싱).
2. 빌드타임 스크립트 `scripts/build-stars.mjs`:
   - 필드 정제 → `{ id, ra, dec, mag, bv, name?, bayer? }`
   - **B-V → RGB**: Ballesteros 공식으로 `temp(K)` 산출 → 흑체 RGB → hex.
   - mag 기준 정렬/필터(밝은 순), `public/data/stars.json`로 출력.
3. 런타임: 한 번 fetch → Float32Array(positions/colors/sizes)로 `Points` 밭 구성.
4. RA/Dec → xyz:
   ```
   const phi = dec * DEG2RAD, theta = ra * (Math.PI/12); // ra in hours
   x = R*cos(phi)*cos(theta); y = R*sin(phi); z = R*cos(phi)*sin(theta);
   ```

---

## 4. 기존 코드 변경점 (작다)

| 파일 | 지금 | 바꿈 |
|---|---|---|
| `components/BackgroundStars.tsx` | 4,000개 랜덤 구면 + 가짜 HSL 색 | **실제 카탈로그 로드** → 진짜 RA/Dec 위치 + 진짜 B-V 색. "숨은 별 밭"이 됨 |
| `store/universe.ts` | `randomStarPosition` 생성 | **`revealStarFromAnswer`**: 감정 톤 → 카탈로그 미점등 별 선택 → 점등 상태로 전환 |
| `components/StarMesh.tsx` | 색=star.color(글자수 유래) | 색=카탈로그 B-V. 이름 없으면 라벨 없음 |
| `components/StarCard.tsx` | 답·질문 표시 | + "이름 붙이기" 액션, (옵션)나중에 진짜 이름 이스터에그 |
| 신규 `lib/starCatalog.ts` | — | 로드·좌표변환·색변환·"구역 내 미점등 별 선택" 유틸 |
| 신규 `lib/bvColor.ts` | — | B-V → RGB |

핵심: **새 엔진이 아니라 데이터 소스 교체.** 비주얼 파이프(글로우·bloom·텍스처)는 그대로 재사용.

---

## 5. 단계별 구현 — PLAN 13의 Phase 0과 1 사이에 끼움

### Phase 0.5 — "진짜 별" 체험 (현 프로토타입 위 1주)
- [ ] BSC5 → `public/data/stars.json` 빌드 스크립트
- [ ] `bvColor.ts` (B-V→RGB)
- [ ] `BackgroundStars`를 실제 카탈로그·실제 위치·실제 색으로 교체 (흐릿한 숨은 밭)
- [ ] 답하기 → 랜덤 생성 대신, 근처 미점등 **진짜 별 점등**(우선 톤매칭 없이 최근접/랜덤 점등으로 시작)
- [ ] 점등 별 탭 → 기억 카드 + 이름 붙이기
- [ ] 배포 → 본인+5명 "또 보고 싶은가?" (PLAN Phase 0 판단 기준 그대로)

### Phase 1에 흡수
- 감정 톤 → 별 선택(Claude API) — PLAN 4-3 파이프와 합침
- 점등 별 영구 저장(Supabase), 개인화 모델 A 확정
- "진짜 이름" 이스터에그(고유명 보유 별 한정, 지연 등장)

이후 Phase 2/3은 PLAN.md 그대로(별자리 잇기·변형 천체·시즌 편지·타임랩스). 전부 실제 별 위에서 더 강해짐.

---

## 6. 본질 가드레일 — 하면 안 되는 것

- ✗ 천문 라벨(별자리 이름·좌표·등급·광년 숫자)을 기본 노출. → 교육앱 변질.
- ✗ "오늘의 별자리 퀴즈", 천문 도감, 망원경 시뮬 같은 기능 추가. → 주제가 '나'에서 '하늘'로 새는 순간 끝.
- ✗ 실제 별 이름을 점등 즉시 보여주기. → 유저의 의미부여를 가로챔.
- ✗ 정확한 하늘 재현에 집착해 비주얼(bloom·성운·느림)을 희생. → 정확도는 이스터에그용, 미감이 우선.
- ✓ 진짜 데이터는 **조용한 진실의 기질**로만. 아는 사람만 알아챈다(PLAN 10장 정신).

---

## 7. 최종 판단

- **현실성**: 높음. 데이터 무료·소형·개방, 렌더링 엔진 기보유, 변경량 작음.
- **본질 정합성**: 랜덤 별보다 **더 정합**. "만들지 않고 드러낸다"가 비유에서 사실로.
- **차별화**: "실제 밤하늘 위에 쌓이는 나의 심리 지도" — 경쟁앱 어디에도 없음. 공유 욕구 자극.
- **위험**: 단 하나, 천문 교육앱으로의 변질. 6장 가드레일로 차단.

→ **진행 권장.** 단 §2-5 개인화 모델만 사용자 확정 후 Phase 0.5 착수.

---

## 8. 사용자 확인 필요한 열린 결정

1. **개인화 모델**: A(공유 하늘·고유 점등, 권장) vs B(고유 시드 패치) vs C(임베딩 좌표).
2. **2D vs 3D 기본 뷰**: 3D 구면(현 엔진 재사용, 권장) vs 순수 2D 평면 천구도.
3. **카탈로그 범위**: BSC5 9천(육안, 권장) vs HYG 1.6만(더 촘촘한 밭).
4. **Phase 0.5를 지금 별도로 진행할지**, 아니면 바로 Phase 1 설계에 흡수할지.
